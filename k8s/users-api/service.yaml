apiVersion: v1
kind: Service
metadata: 
  name: users-api
spec:
  type: NodePort # Accessible outside the cluster, unlike ClusterIP
  selector:   
    app: users-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30002 # 30000–32767 KUBERNETES DEFAULT
